anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: protocolbuffers/protobuf
      use_max_tag: true
      include_regex: ^v?\d+\.\d+$
      from_pattern: ^v?(\d+\.\d+)$
      to_pattern: 7.\1
