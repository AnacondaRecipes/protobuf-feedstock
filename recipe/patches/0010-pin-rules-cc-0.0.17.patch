--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -25,6 +25,12 @@ bazel_dep(name = "rules_proto", version
 # protoc dependencies
 bazel_dep(name = "abseil-cpp", version = "20250512.1")
 bazel_dep(name = "rules_cc", version = "0.0.17")
+# Force rules_cc 0.0.17: newer BCR resolutions (0.2.15) need _cc_internal.freeze
+# which is missing under conda bazel 8.6 on Windows and breaks analysis.
+single_version_override(
+    module_name = "rules_cc",
+    version = "0.0.17",
+)
 bazel_dep(name = "zlib", version = "1.3.1.bcr.5")
 #ifndef PROTO2_OPENSOURCE
 # LINT.ThenChange(//depot/google3/third_party/protobuf/compiler/notices.h)
